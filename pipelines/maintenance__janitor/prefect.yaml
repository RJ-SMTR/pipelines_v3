name: maintenance__janitor
prefect-version: 3.4.8

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false
  - prefect_docker.deployments.steps.build_docker_image:
      id: build-image
      requires: prefect-docker>=0.6.5
      image_name: ghcr.io/rj-smtr/pipelines_v3/deployments
      tag: "maintenance__janitor-{{ get-commit-hash.stdout }}"
      dockerfile: pipelines/maintenance__janitor/Dockerfile

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.6.5
      image_name: "{{ build-image.image_name }}"
      tag: "{{ build-image.tag }}"

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /opt/prefect/pipelines_v3

deployments:
  - name: rj-maintenance--janitor--staging
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/maintenance__janitor/flow.py:maintenance__janitor
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package maintenance__janitor -- prefect flow-run execute
        secretName: prefect-jobs-secrets-staging
        image_pull_policy: Always

  - name: rj-maintenance--janitor--prod
    version: "{{ get-commit-hash.stdout }}"
    entrypoint: pipelines/maintenance__janitor/flow.py:maintenance__janitor
    schedules:
      - cron: "0 0 * * *"
        timezone: "America/Sao_Paulo"
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package maintenance__janitor -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always

  - name: rj-maintenance--db-retention--staging
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/maintenance__janitor/flow.py:maintenance__retention_flow
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package maintenance__janitor -- prefect flow-run execute
        secretName: prefect-jobs-secrets-staging
        image_pull_policy: Always

  - name: rj-maintenance--db-retention--prod
    version: "{{ get-commit-hash.stdout }}"
    entrypoint: pipelines/maintenance__janitor/flow.py:maintenance__retention_flow
    schedules:
      - cron: "0 0 * * *"
        timezone: "America/Sao_Paulo"
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package maintenance__janitor -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always
