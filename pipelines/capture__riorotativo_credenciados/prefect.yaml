name: capture__riorotativo_credenciados
prefect-version: 3.4.8

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false
  - prefect_docker.deployments.steps.build_docker_image:
      id: build-image
      requires: prefect-docker>=0.6.5
      image_name: ghcr.io/rj-smtr/pipelines_v3/deployments
      tag: "capture__riorotativo_credenciados-{{ get-commit-hash.stdout }}"
      dockerfile: pipelines/capture__riorotativo_credenciados/Dockerfile

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.6.5
      image_name: "{{ build-image.image_name }}"
      tag: "{{ build-image.tag }}"

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /opt/prefect/pipelines_v3

deployments:
  - name: rj-capture--riorotativo_credenciados--staging
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/capture__riorotativo_credenciados/flow.py:capture__riorotativo_credenciados
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package capture__riorotativo_credenciados -- prefect flow-run execute
        secretName: prefect-jobs-secrets-staging
        image_pull_policy: Always
  - name: rj-capture--riorotativo_credenciados--prod
    version: "{{ get-commit-hash.stdout }}"
    entrypoint: pipelines/capture__riorotativo_credenciados/flow.py:capture__riorotativo_credenciados
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package capture__riorotativo_credenciados -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always
