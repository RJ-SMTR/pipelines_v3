name: capture__stu_tabelas
prefect-version: 3.4.8

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false
  - prefect_docker.deployments.steps.build_docker_image:
      id: build-image
      requires: prefect-docker>=0.6.5
      image_name: ghcr.io/rj-smtr/pipelines_v3/deployments
      tag: "capture__stu_tabelas-{{ get-commit-hash.stdout }}"
      dockerfile: pipelines/capture__stu_tabelas/Dockerfile

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.6.5
      image_name: "{{ build-image.image_name }}"
      tag: "{{ build-image.tag }}"

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /opt/prefect/pipelines_v3

deployments:
  - name: rj-capture--stu_tabelas--staging
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/capture__stu_tabelas/flow.py:capture__stu_tabelas
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package capture__stu_tabelas -- prefect flow-run execute
        secretName: prefect-jobs-secrets-staging
        image_pull_policy: Always
        cpu_limit: "1000m"
        memory_limit: "4600Mi"
        cpu_request: "500m"
        memory_request: "1000Mi"
  - name: rj-capture--stu_tabelas--prod
    version: "{{ get-commit-hash.stdout }}"
    entrypoint: pipelines/capture__stu_tabelas/flow.py:capture__stu_tabelas
    schedules:
      - cron: "20 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [combustivel]
      - cron: "21 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [controle_processo]
      - cron: "22 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [darm_apropriacao]
      - cron: "23 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [mod_carroceria]
      - cron: "24 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [mod_chassi]
      - cron: "25 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [multa]
      - cron: "26 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [permissao]
      - cron: "27 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [pessoa_fisica]
      - cron: "28 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [pessoa_juridica]
      - cron: "29 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [planta]
      - cron: "30 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [talonario]
      - cron: "31 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [tipo_de_permissao]
      - cron: "32 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [tipo_de_permissionario]
      - cron: "33 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [tipo_de_transporte]
      - cron: "34 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [tipo_de_veiculo]
      - cron: "35 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [veiculo]
      - cron: "36 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [veiculo_ativo]
      - cron: "37 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [vistoria]
      - cron: "38 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [guia]
      - cron: "39 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [permis_empresa_escola]
      - cron: "40 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [modelo]
      - cron: "41 8 * * *"
        timezone: America/Sao_Paulo
        parameters:
          recapture: true
          source_table_ids: [multa_postagem]
    work_pool:
      name: smtr-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package capture__stu_tabelas -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always
        cpu_limit: "1000m"
        memory_limit: "4600Mi"
        cpu_request: "500m"
        memory_request: "1000Mi"
